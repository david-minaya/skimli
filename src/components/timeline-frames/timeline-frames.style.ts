import { Style } from '~/utils/style';

export const style = Style({
  frames: {
    width: '100%',
    height: '48px',
    display: 'flex',
    position: 'absolute',
    bottom: '0px',
    backgroundColor: 'aliceblue'
  }
});
