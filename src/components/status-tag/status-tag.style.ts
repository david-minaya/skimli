import { Style } from '~/utils/style';

export const style = Style({
  tag: {
    color: 'white',
    fontSize: '11px',
    fontWeight: 500,
    cursor: 'default',
    borderRadius: '100px',
    padding: '2px 8px',
    userSelect: 'none',
    backgroundColor: 'gray'
  }
});
